Any list that can be empty gets one — search with no matches, no applications yet, empty verification queue.

```jsx
<EmptyState icon="search-x" title="Không tìm thấy tin phù hợp"
  description="Thử bỏ bộ lọc mức lương hoặc mở rộng khu vực."
  action={<Button variant="secondary">Xóa bộ lọc</Button>} />
```

The title states the fact, the description offers the fix. Never apologise, never blame the student.
