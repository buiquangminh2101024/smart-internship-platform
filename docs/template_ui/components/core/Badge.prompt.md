Static metadata label: counts, industries, skills, "Mới", "Đã xác thực". For job-post lifecycle state use `StatusPill` instead; for actor roles use `RoleBadge`.

```jsx
<Badge tone="brand" icon="check">Đã xác thực</Badge>
<Badge tone="accent">Mới</Badge>
<Badge>Toàn thời gian</Badge>
```
