# Project Status

Trạng thái hiện tại của từng phase trong roadmap (`PROJECT_PHASES.md`). Cập nhật mỗi khi một phase đổi trạng thái — không phải nhật ký chi tiết từng thay đổi (xem lịch sử qua `git log`).

| Phase | Trạng thái | Ghi chú |
|---|---|---|
| 0 — Foundation & Environment | ✅ Hoàn thành | `.gitignore`, `docs/`, Nginx + Redis trong `infra/docker-compose.yaml` |
| 1 — Core Architecture & Data Layer | ✅ Hoàn thành | Prisma schema đầy đủ, kết nối Neon, awilix composition root, health module. Có 1 delta bổ sung sau đó cho auth (xem dưới) |
| 2 — Identity & Access (backend) | ✅ Hoàn thành | Kế hoạch chi tiết: `docs/phases/phase-02-identity-access/PLAN.md`. Đã triển khai module `auth`/`users`, JWT access+refresh (blacklist qua Redis khi logout), OTP qua Redis (`OTP_HARDCODE` hỗ trợ dev), rate-limit OTP theo email + IP, Google OAuth (verify + auto-link theo email), forgot/reset password. Đã kiểm thử thủ công end-to-end qua curl (register/verify-otp/login/refresh/logout/`/users/me`/Admin login/chặn `role=ADMIN`) trên Neon + Redis Cloud thật. |
| 2 — Identity & Access (frontend) | ✅ Hoàn thành | Kế hoạch: `docs/05-frontend/phases/phase-02-identity-access/PLAN.md` + mở rộng phạm vi ở AD-3 (`docs/02-architecture/ARCHITECTURE_DECISIONS.md`). Đã có: `proxy.ts` (guard theo actor — xem deviation bên dưới), `lib/api-client.ts` (refresh-on-401), `stores/auth-store.ts` (zustand+persist), form đăng ký/đăng nhập/OTP dùng chung Candidate/Employer, Google Identity Services, `/admin` login riêng, 3 trang chủ theo actor (`/`, `/employer`) dựng đầy đủ theo AD-3, `components/ui/*` (Button/Card/Input/Select/Badge/StatCard/JobCard/RoleBadge/Icon dựa trên `lucide-react`). Đã build (`next build`) và kiểm thử thủ công luồng register→verify-otp→`/users/me`→login→refresh→logout qua curl trực tiếp backend thật (Neon+Redis Cloud), cùng smoke-test toàn bộ route qua `next start`/`next dev` (200 cho trang công khai, redirect 307 đúng đích khi vào route cần role chưa đăng nhập). **Chưa kiểm thử qua trình duyệt thật** (không có công cụ browser trong phiên làm việc này) — luồng Google OAuth và click-through OTP trên UI cần chủ dự án tự kiểm tra thủ công trước khi coi là xong hoàn toàn. Deviation so với plan gốc: (1) file đổi tên `middleware.ts` → `proxy.ts`, export `proxy` thay vì `middleware` — Next.js 16 đổi quy ước này, không phải quyết định của Phase 2; (2) vì token nằm ở `localStorage` (AD-2) nên `proxy.ts` (chạy phía server) không đọc được — bổ sung cookie không nhạy cảm `sip_role` (chỉ chứa role) do `stores/auth-store.ts` ghi song song để proxy chặn route theo actor, xem chú thích trong `lib/auth-storage.ts`; (3) áp dụng nhất quán quy tắc "chỉ đổi navbar theo trạng thái đăng nhập" (đã chốt cho `/`) sang cả `/employer` và `/admin`, vì đích redirect sau đăng nhập Employer/Admin tạm thời chính là các trang này (portal/console chưa có nội dung) — nếu không, trang sẽ hiện lại y hệt như chưa đăng nhập ngay sau khi đăng nhập thành công. **Đợt sửa lỗi sau rà soát lại (2026-09-02):** đã bổ sung `SessionSync` (`useQuery(["me"])`, đúng như plan gốc dòng 51 nhưng bị bỏ sót lúc triển khai) để re-verify session mỗi lần load app thay vì chỉ tin `user` cũ trong `localStorage`; thêm `hasHydrated` vào `auth-store.ts` để tránh nhấp nháy nội dung khách/đã đăng nhập ở header `/` và `/employer` trong lúc chờ zustand persist rehydrate; `Input.tsx` tự sinh `id` qua `useId()` để `<label>` luôn gắn đúng input (trước đó không call site nào truyền `id`, label không liên kết với field ở bất kỳ form nào); `completeAuth` dọn lại session nếu `/users/me` lỗi sau login/verify-otp/google (tránh state lỡ dở: có `accessToken` nhưng không có `user`); `/admin` không còn tạo session thật (token + cookie `sip_role`) cho tài khoản Candidate/Employer hợp lệ trước khi phát hiện sai role — dùng `fetchProfileWithToken` kiểm tra role trước, chỉ commit session khi đúng `ADMIN`; dọn metadata mặc định của `create-next-app` (`lang="en"` → `"vi"`, title/description) và bổ sung `metadata` riêng cho `/admin` và 5 trang placeholder Candidate. Thông báo lỗi backend còn tiếng Anh hiển thị thẳng trong UI tiếng Việt (case sai mật khẩu, rate-limit, tài khoản bị khoá...) — biết nhưng chủ động chưa sửa theo yêu cầu chủ dự án. |
| 3 — Candidate Profile | 🔄 Đang thực hiện (nhóm khác) | |
| 4 — Employer & Company Module | ✅ Hoàn thành | Kế hoạch: `docs/06-backend/phase-04-employer-company/PLAN.md`, `docs/05-frontend/phases/phase-04-employer-company/PLAN.md`. Quyết định kiến trúc: `ARCHITECTURE_DECISIONS.md` AD-5. Đã triển khai: module `employers`/`companies`/`catalog` (backend), luồng hoàn tất thủ tục `employer/hoan-tat-thu-tuc` + trang `employer/profile` + màn quản lý `admin/companies` (frontend). Xác thực công ty tự động khớp mã số thuế (VietQR) hoặc thủ công qua Admin (kèm giấy phép kinh doanh), liên kết công ty đã có qua mã mời 6 số (Redis, TTL 2 phút). Đã kiểm thử thủ công end-to-end qua curl trên Neon+Redis Cloud+Cloudinary thật (đăng ký công ty mới luồng auto/manual/blocked+forceManualReview, admin verify/reject/resubmit, invite-code issue/join/single-use/expiry, toggle `requiresApproval`, role guard 401/403, chặn khởi động khi `DEV_SKIP_COMPANY_MANUAL_VERIFICATION=true` lúc `NODE_ENV=production`). **Chưa kiểm thử qua trình duyệt thật** (không có công cụ browser trong phiên làm việc này) — chủ dự án cần tự kiểm tra UI (`next build` đã pass, routes render đúng theo cây route) trước khi coi là xong hoàn toàn. Bug tìm thấy & sửa cùng đợt (không thuộc phạm vi nghiệp vụ Phase 4): `shared/middleware/validate.ts` không hoạt động với `part: "query"` trên Express 5 (getter-only `req.query`); `z.coerce.boolean()` coerce sai chuỗi `"false"` thành `true` (đã tránh dùng cho cờ `DEV_SKIP_COMPANY_MANUAL_VERIFICATION` mới, không sửa `OTP_HARDCODE` cũ vì ngoài phạm vi task). |
| 5 — Subscription & Payment cho đăng tin tuyển dụng | 🔄 Đang thực hiện (lỗi VNPay sandbox chưa giải quyết được) | Kế hoạch: `docs/06-backend/phase-05-subscription-payment/PLAN.md`, `docs/05-frontend/phases/phase-05-subscription-payment/PLAN.md`. Thiết kế: `docs/designs/SUBSCRIPTION_BILLING_DESIGN.md`, quyết định: `ARCHITECTURE_DECISIONS.md` AD-6. Đã triển khai: module `payments` (`PaymentGatewayAdapter` + `VnpayGatewayAdapter`/`MomoGatewayAdapter`, `shared/utils/payment-signing.ts`, IPN handler có idempotency + log mọi callback) và `subscriptions` (`SubscriptionsService.getCompanySubscriptionAccess` — trial/quota/blocked, checkout, sweep hết hạn qua `node-cron` mỗi giờ), seed 2 `PaymentMethod` + 3 `SubscriptionPlan` mẫu, frontend `/employer/subscription` (danh sách gói + lịch sử) + 2 trang return (poll trạng thái thật, không tin query param) + khối "Gói dịch vụ" ở `/employer/profile` (chỉ hiện khi công ty đã `VERIFIED`). Đã build sạch cả `apps/server` (`tsc`) lẫn `apps/web` (`next build`, đúng route tree `/employer/subscription`, `/employer/subscription/return/{vnpay,momo}`), xác nhận `prisma migrate status` không cần migration mới, và kiểm thử thủ công qua curl trên Neon thật: `GET /subscription-plans` trả đúng dữ liệu đã seed, cả 2 endpoint IPN từ chối đúng cách khi chữ ký sai (`vnpay` trả `200 {"RspCode":"97"}`, `momo` trả `400`). **⚠️ CHƯA XONG — lỗi trang thanh toán VNPay sandbox thật vẫn CHƯA SỬA ĐƯỢC:** khi test checkout thật qua trình duyệt, trang VNPay sandbox trả lỗi `Error.html?code=71` ("Website chưa được phê duyệt"). Đã tìm và sửa 1 bug thật liên quan (`vnp_IpAddr` dạng IPv4-mapped IPv6 `::ffff:127.0.0.1` bị VNPay từ chối, xem AD-6) nhưng sau khi sửa lỗi `code=71` vẫn còn — nhiều khả năng do tài khoản merchant sandbox mượn (`PG3V5QW9`) chưa được duyệt cho website/domain của dự án này, chưa xác nhận chắc chắn được. Đã thêm cờ tạm `DEV_SKIP_PAYMENT_GATEWAY` (env, mặc định `false`, chặn cứng khi `NODE_ENV=production`) để bỏ qua bước gọi gateway thật và tiếp tục phát triển/test các phần phụ thuộc subscription trong lúc chờ xử lý — **việc thanh toán VNPay/Momo thật vẫn cần sửa và kiểm thử lại qua trình duyệt trước khi coi Phase 5 là hoàn thành.** Deviation so với plan gốc (không đổi hành vi, chỉ tổ chức lại cho khớp convention sẵn có của repo): `EmployerRepository` chuyển đăng ký từ `employers.routes.ts` sang `container.ts` (dùng chéo bởi `subscriptions`, cùng lý do `CompanyRepository` đã tập trung từ Phase 4); `paymentsService`/`subscriptionsService`/các repository riêng module vẫn đăng ký trong `*.routes.ts` của module mình (không dồn hết vào `container.ts` như bản nháp PLAN.md Phần 3 bước 8 — chỉ 2 gateway adapter + map `paymentGatewayAdapters` mới thật sự infra-level nên mới tập trung); `PaymentsService.createCheckout` nhận `companySubscriptionId` (không phải `paymentId`) và tự tạo `Payment`/`Transaction` — khớp đúng câu "tạo CompanySubscription(PENDING) + gọi PaymentsService" ở Phần 2 gốc, giữ ranh giới rõ hơn (module `payments` sở hữu trọn `Payment`/`Transaction`); không tạo `payments.dto.ts` (2 route IPN không có body cần Zod validate, xác thực bằng chữ ký); `MOMO_REQUEST_TYPE=payWithATM` (biến env mới, giá trị mặc định lấy từ cấu hình đang hoạt động của `event-ticketing-platform`, không phải `captureWallet` như phác thảo ban đầu ở `SUBSCRIPTION_BILLING_DESIGN.md` §6.3 — không cần app Momo Wallet để test trên sandbox web). |
| 6 — Job Recruitment Module | ✅ Hoàn thành | Kế hoạch: `docs/06-backend/phase-06-job-recruitment/PLAN.md`, `docs/05-frontend/phases/phase-06-job-recruitment/PLAN.md`. Đã hoàn thành theo luồng của project. |
| 7 — CV & Saved Jobs | ✅ Hoàn thành | Kế hoạch/Implementation: `docs/06-backend/phase-07-cv-saved-jobs/IMPLEMENTATION.md`, `docs/05-frontend/phases/phase-07-cv-saved-jobs/IMPLEMENTATION.md`. Đã hoàn thành các chức năng upload/xoá/quản lý CV, lưu/bỏ lưu tin tuyển dụng. Tích hợp trực tiếp Cloudinary MediaStorageService. Cập nhật trang Job Detail liên kết thật với Backend thay vì mock state. |
| 8 — Application Module | ⬜ Chưa bắt đầu | |
| 9 — Realtime Communication | ⬜ Chưa bắt đầu | |
| 10 — Notification & Email | ⬜ Chưa bắt đầu | |
| 11 — AI Features Boundary | ⬜ Chưa bắt đầu | |
| 12 — Integration & Security Hardening | ⬜ Chưa bắt đầu | |
| 13 — Testing & Quality | ⬜ Chưa bắt đầu | |
| 14 — Deployment & Thesis Preparation | ⬜ Chưa bắt đầu | |

## Delta ngoài roadmap gốc

- **Auth bootstrap sớm (trước khi Phase 2 chính thức bắt đầu code):** đã chốt quyết định Admin không có luồng đăng ký (chỉ tạo qua `apps/server/scripts/create-admin.ts`) và Candidate/Employer hỗ trợ cả email/password lẫn Google OAuth. Đã áp dụng delta này vào schema (migration `20260901124902_admin_auth_oauth_prep`) và tài liệu kiến trúc trước khi viết code Phase 2 thật, để tránh phải sửa schema/migration lại sau.
- **2026-09-09 — sửa lệch số phase:** bảng ở trên (và `docs/05-frontend/FRONTEND_PHASES.md`) trước đó đánh số 5-13 theo roadmap cũ (trước khi Phase "Subscription & Payment" được chèn chính thức làm Phase 5 trong `PROJECT_PHASES.md`/`SUBSCRIPTION_BILLING_DESIGN.md`) — đã đánh số lại cho khớp, không phải thay đổi nội dung roadmap.
